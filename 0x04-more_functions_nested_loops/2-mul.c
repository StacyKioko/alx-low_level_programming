#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 *
 * Return: The result of multiplication of x param and y
 */
int mul(int x, int y)
{
	return (x * y);
}
